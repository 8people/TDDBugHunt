<% if all_errors.any? %>
<div id="error_explanation" class="error_explanation">
    <h2><%= pluralize(all_errors.count, "error") %> prohibited this Task List from being saved:</h2>

    <ul>
    <% all_errors.each do |error| %>
        <li><%= error %></li>
    <% end %>
    </ul>
</div>
<% end %>